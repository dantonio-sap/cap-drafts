-- generated by cds-compiler version 2.13.8
COLUMN TABLE my_bookshop_BookCosts (
  ID INTEGER NOT NULL,
  book_ID INTEGER,
  purchasePrice INTEGER,
  salesPrice INTEGER,
  PRIMARY KEY(ID)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN my_bookshop_Books AS book ON (book.ID = book_ID)
)